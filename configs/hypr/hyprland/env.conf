# ############ Wayland (battery: XWayland costs wakeups, prefer native) #############
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = MOZ_ENABLE_WAYLAND,1
env = GDK_BACKEND,wayland
env = SDL_VIDEODRIVER,wayland
env = QT_QPA_PLATFORM,wayland
# NOTE: no QT_QPA_PLATFORMTHEME (no KDE/qt6ct on Minimal; qt defaults are fine)

######### Applications #########
env = XDG_DATA_DIRS,$HOME/.local/share/flatpak/exports/share:/var/lib/flatpak/exports/share:/usr/local/share:/usr/share
env = XDG_CURRENT_DESKTOP,Hyprland

# ######## Terminal application #########
env = TERMINAL,foot
